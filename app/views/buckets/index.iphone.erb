<ul id="buckets" title="Buckets" selected="true">
  <% for bucket in @buckets %>
    <li><%= link_to("#{bucket.name}: #{number_to_currency(bucket.balance)}", bucket) %></li>
  <% end %>
    <li><%= link_to("Add a Bucket", new_bucket_path) %></li>
</ul>

